import styled from 'styled-components';
import {
  color,
  layout,
  space,
  typography,
  flexbox,
  grid,
  border,
} from 'styled-system';

export const Box = styled('div')(
  color,
  layout,
  space,
  typography,
  flexbox,
  grid,
  border
);
